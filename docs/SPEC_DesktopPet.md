# 桌面宠物规格说明书（PetAliveTools / 视频精灵路线）

> 版本：v0.1　｜　日期：2026-08-13　｜　状态：访谈定稿，待实施
> 本规格基于一轮结构化访谈得出。所有"**决策**"条目均为用户明确选择；"**提案**"条目为基于决策的补充设计，标注待确认。

---

## 0. 决策摘要（一表速览）

| 维度 | 决策 |
|---|---|
| 核心路线 | **拍真实视频还原**（非建模、非逐帧生成） |
| 身份内涵 | 外观 + 性格气质 + 标志性动作习惯 + 声音（全维度） |
| 行为引擎 | **确定性状态机**（无 LLM） |
| 运行架构 | **全本地、完全离线** |
| 素材来源 | 活体宠物，**可随时补拍**（迭代式入库） |
| 抠像 | **纯色背景 + 色键抠像**（最高边缘质量） |
| 动作衔接 | **锚定姿态中转**（每段以中性姿态起止） |
| 素材入库 | **清单引导 + 手动导入打标** |
| 行为范围 | 基础生命 + 互动响应 + 个性招牌 + 情绪/需求（全覆盖） |
| 空间模型 | **底部地面条带行走** |
| 交互 | 鼠标抚摸 + 点击呼应 + 拖拽移动 + 喂食/玩具 UI |
| 反循环感 | 多变体+稀有动作 / 全局呼吸缩放 / 调度微随机 / 补拍缓解（并用） |
| 技术栈 | **Electron（Web）** |
| 缺素材兜底 | **占位片段 + 采集清单标红提醒** |
| 桌面集成 | **始终置顶**（+ 全局快捷键临时隐藏安全阀，见 §13） |
| 音频 | **节律随机环境声**（昼夜节律，频率可调） |

---

## 1. 产品愿景与核心命题

把用户自己的真实宠物转化为 Windows 桌面常驻伴侣，追求三个目标：

1. **高身份一致性**——它就是"那只"，不是"一只像它的"。
2. **自然动作连续性**——动作之间无缝衔接，看不出"视频拼接"。
3. **基础生命感**——它像在过自己的生活，而非循环播放的 GIF。

### 1.1 路线选择的根本权衡

用户否决了所有建模路线（3D / 2D 绑定 / 扩散逐帧），理由是"任何建模都还原不了我自己的真实宠物"。由此采用**视频精灵（Video Sprite）路线**：拍真实视频 → 抠出透明前景 → 状态机按需播放/衔接 → 叠在桌面。

该路线天然保住全部身份维度（视频同时含外观/动作/声音/气质），但引入三个固有难题，本规格的核心工程量都围绕它们：

- **D1 毛发抠像**：纯色背景色键可解，但需规范采集（§4、§5）。
- **D2 离散片段 vs 连续性**：用锚定姿态中转机制（§8）。
- **D3 视频无法程序化叠加微动作**：无法像 3D 那样在固定画面上加呼吸/眨眼层，故"生命感"只能靠素材多样性 + 调度随机性近似（§9.5）。

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      离线采集 / 入库管线                         │
│  拍摄(纯色背景) → 色键抠像 → 裁剪/标loop → 转码WebM-alpha        │
│                  → 打标(state/方向/anchor/个性tag) → 入库        │
│                  （清单引导式导入 UI，占位+采集提醒）              │
└─────────────────────────────────────────────────────────────┘
                            │  pet 项目目录（资产 + 元数据）
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                       本地运行时（Electron）                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │ 渲染层    │←│ 空间层    │←│ 调度层    │←│ 行为引擎FSM   │   │
│  │ 透明置顶窗 │  │ 地面条带  │  │ 片段选择  │  │ 需求/节律/性格 │   │
│  │ WebM-alpha│  │ 方向/尺度 │  │ 锚定中转  │  │ 转移概率     │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘   │
│        ↑ 交互层(抚摸/点击/拖拽/喂食)    ↓ 音频层(节律环境声)       │
└─────────────────────────────────────────────────────────────┘
```

**模块划分**：采集入库（pipeline）、资产库（asset store）、行为引擎（behavior）、空间运动（spatial）、调度衔接（scheduler）、渲染合成（render）、交互（input）、音频（audio）、持久化（persistence）、外壳（shell：托盘/自启/设置）。

---

## 3. 技术栈

**决策：Electron（Web 技术）**

### 3.1 选型理由
- 透明带 Alpha 通道视频播放是本项目的第一约束。Electron（Chromium）的 `<video>` 原生支持 **WebM（VP8/VP9 带 alpha）**，是所有候选方案里透明视频最现成的路径。
- Web 生态成熟，FSM/调度/UI 用 TS 开发效率高。
- 跨窗口、托盘、全局快捷键、自启均有成熟 API。

### 3.2 已知代价与缓解
| 代价 | 缓解 |
|---|---|
| 常驻内存偏重（基线 ~120–180MB） | 单窗口、禁用多余模块；仅一个隐藏 BrowserWindow + 一个透明宠物窗口；空闲时降低调度频率。 |
| 电池/ CPU | 仅解码当前播放的单段循环，硬解优先（§14）；空闲态拉长调度间隔。 |
| 包体大 | 用 electron-builder，必要时仅打包 win-x64。 |

### 3.3 关键依赖（提案）
- 视频：`<video>` + WebM(VP9-alpha)；解码/拼接用 `ffmpeg`（打包二进制，离线）做入库转码。
- 透明窗口：`BrowserWindow { transparent:true, frame:false, alwaysOnTop:true, setIgnoreMouseEvents }`。
- 全局快捷键：`globalShortcut`（隐藏安全阀）。
- 自启：`auto-launch`。

---

## 4. 素材采集规范（拍摄清单）

> 视频路线"垃圾进垃圾出"。本节是质量的源头，也是用户的拍摄手册。**提案**：内置一份可勾选的拍摄清单（与 §5 导入 UI 同源）。

### 4.1 硬性拍摄规范
- **背景**：单一均匀纯色（推荐中灰/中蓝布幕；避免与宠物毛色相近）。背景需静态、无阴影、无杂物。绿幕可选但非必须（纯色色键足够）。
- **光照**：均匀漫射，避免硬阴影落在背景上（阴影会破坏色键）。正面/侧面补光，宠物身上无过曝。
- **机位**：固定三脚架；**水平侧机位**（宠物侧面朝向镜头，便于"在桌面上行走"的阅读感）；机位距离全程一致（保证尺度统一）。
- **地面**：拍摄台面即为"地面"，所有片段同一地面线高度。
- **参数**：1080p 及以上，30–60fps，每段时长 3–10 秒（行走类 5–8 秒便于覆盖屏幕宽度）。

### 4.2 锚定姿态纪律（连续性的基础）
- **锚定姿态**：端坐正脸、放松、面向镜头（或统一约定为"站立侧面"）。全文统一一种。
- **每段非循环动作**必须 `[锚定] → [动作] → [锚定]` 起止；状态切换经锚定中转（§8）。
- **循环类片段**（如理毛、睡眠呼吸）需标注无缝 loop 入/出点（§5.3）。

### 4.3 方向与不对称花纹（关键！）
- 沿底部条带行走需要**左行、右行**两个方向。
- **决策性提案**：对**花纹不对称**的宠物（如身上一块斑、折耳单侧、断尾），**禁止用镜像翻转生成反方向**——必须**实拍两个方向**。镜像会让不对称特征翻边，破坏身份一致性。
- 对称宠物可只拍一个方向、运行时镜像（节省一半采集量）。在入库时标记 `symmetrical: true/false`。

### 4.4 完整拍摄清单（按四大行为类别）

> 必拍=最小可用集；选拍=丰富生命感；个性=你宠物独有。每项尽量 2–3 个变体以反循环（§9.5）。

**A. 基础生命状态（必拍，构成 FSM 骨架）**
| 动作 | 必/选 | 方向 | 说明 |
|---|---|---|---|
| 端坐（锚定） | 必拍 | 正面 | 所有中转的基准姿态 |
| 站立 | 必拍 | 侧面 | 起身/过渡 |
| 行走 | 必拍 | 左+右 | 5–8 秒，覆盖屏幕宽度 |
| 趴卧 | 必拍 | — | 静止/呼吸 loop |
| 睡眠 | 必拍 | — | 长循环，含翻身变体 |
| 理毛 | 选拍 | — | loop |
| 转身 | 必拍 | 双向 | 边缘转向用 |
| 起身/趴下过渡 | 必拍 | — | 锚定↔趴卧 |

**B. 互动响应（选拍，增强连接）**
- 被抚摸享受（眯眼/呼噜/蹭手）｜抬头看镜头（点击呼应）｜被呼唤转身｜被抱起/悬空（拖拽反应）｜松手落回地面

**C. 个性招牌动作（个性，身份灵魂）**
- 由用户自定义捕捉：翻肚皮 / 叼玩具 / 打呼噜 / 特定怪睡姿 / 蹭人 / 冲刺跑酷……每项打上 `signature` 标签，低频偶发触发。

**D. 情绪/需求表达（选拍，养成感）**
- 讨食 / 喝水 / 求玩 / 无聊 / 开心 / 烦躁

### 4.5 反循环素材（§9.5 配套）
- 每个**高频闲置状态**拍 **2–3 个变体**（如 3 种不同坐姿/睡姿）。
- 拍一组**稀有动作**：哈欠 / 伸懒腰 / 挠痒 / 抖耳 / 吐舌，低概率偶发插入。

---

## 5. 抠像与入库管线

### 5.1 色键抠像
- **算法（提案）**：HSV/YCbCr 色键，取背景参考色 → 容差阈值 → 生成 alpha 蒙版。
- **边缘处理**：色溢出抑制（去边缘背景色污染）、alpha 蒙版轻微收缩+羽化（消除毛边杂色）。
- **毛发**：纯色背景下色键对硬边缘可靠；半透明绒毛会有损失——**这是质量悬崖**，入库 UI 提供**抠像预览 + 边缘放大检查**，质量差则引导重拍（调背景色/补光）。
- **背景参考帧**：要求用户先拍 1–2 秒空背景作为参考帧（支持背景减除辅助，提升边缘稳定性）。

### 5.2 转码
- 输出 **WebM（VP9 + alpha）**，统一帧率（如 30fps）、统一分辨率（按宠物身高归一化，§7.4）。
- 比特率平衡体积与质量；长片段（睡眠）可降码率。
- 工具：打包 `ffmpeg`（离线），入库时一次性转码。

### 5.3 片段裁剪与 loop 标注
- 裁掉首尾准备动作，保留 `[锚定]→[动作]→[锚定]`（非循环）或精确 loop 段（循环）。
- 循环片段标注 `loopIn`/`loopOut` 帧或时间码，确保无缝。

### 5.4 打标 schema（片段元数据）
```jsonc
{
  "id": "walk_right_01",
  "state": "walk",            // FSM 状态键
  "category": "basic",        // basic|interactive|signature|emotion
  "direction": "right",       // left|right|none
  "anchor": true,             // 是否锚定起止
  "loop": false,              // 是否循环片段
  "loopIn": null, "loopOut": null,
  "signature": false,         // 个性招牌（低频触发）
  "variants": 1,              // 同状态变体编号
  "symmetrical": false,       // 宠物是否对称（全局，见 §4.3）
  "audio": "meow_02",         // 关联音频素材 id（可空）
  "scaleHint": 1.0            // 尺度归一化用
}
```

### 5.5 清单引导式导入 UI
- 导入向导按 §4.4 清单分组展示，每项状态显示**已入库变体数 / 建议数**。
- **缺素材兜底（决策：占位+采集提醒）**：缺失的关键状态在清单中标红，运行时用通用占位片段（端坐）临时顶替，并在托盘/面板提示"还差哪些动作"。
- 每段导入流程：选视频 → 圈选背景参考色/帧 → 抠像预览 → 裁剪/标 loop → 填标签 → 转码入库。

---

## 6. 渲染层

### 6.1 透明置顶窗口
- 主宠物窗口：`transparent:true, frame:false, alwaysOnTop:true, resizable:false, skipTaskbar:true`。
- **可穿透点击**：默认 `setIgnoreMouseEvents(true, {forward:true})` 让点击落到桌面；当光标进入宠物身体命中盒时切换为可交互（`false`）。
- 命中盒：由当前片段附带的简单矩形/多边形 hitbox 定义（入库时标注或按精灵尺寸估算）。

### 6.2 精灵合成
- `<video>` 元素播放当前 WebM-alpha 片段；通过 CSS `transform: translate(x,y) scale(s) scaleX(±1)` 控制屏幕位置/尺度/方向（镜像仅对对称宠物）。
- 多层叠加：宠物层 + 可选的极轻微全局呼吸缩放（§9.5）。

### 6.3 全局呼吸缩放近似
- 对整个精灵施加极小幅（如 ±0.6%）、低频（~0.25Hz）的 scale 振荡，近似呼吸节奏。
- **局限**：只是整体缩放，宠物画面本身不会真实胸腹起伏；仅供近距离"不循环感"，不可夸大。

### 6.4 多显示器 / DPI / 分辨率
- 默认主显示器；设置项可选显示器。
- 地面线随任务栏位置/显示器高度自适应（§7）。
- 高 DPI 下 WebM 按归一化尺度渲染，保证清晰。

---

## 7. 空间与运动层（底部地面条带）

### 7.1 地面线
- 地面线 = 任务栏上沿（任务栏隐藏时=屏幕底部）。宠物足部始终贴合地面线。
- 宠物在地面线上方区域活动（高度不超过屏幕 ~40%）。

### 7.2 行走映射
- 行走片段内部宠物从画面一侧走到另一侧；运行时把**片段内位移映射为屏幕 x 位移**：播放期间窗口 `translateX` 随片段时间进度线性推进（速度由片段时长与目标屏幕跨度标定）。
- 每个行走片段入库时记录 `pixelsPerSecond`（标定值）。

### 7.3 方向与边缘转身
- 携带 `direction` 标签选择左行/右行片段；对称宠物可镜像。
- **边缘转身**：到达屏幕左右边界时 → 播放"转身"片段（或经锚定中转：站定→转身→反向行走）→ 改变朝向继续。
- 不对称宠物必须已拍转身片段；缺失则停在边界等下次调度（兜底）。

### 7.4 尺度归一化
- 所有片段入库时把宠物**肩高**归一化到统一屏幕占比（如屏幕高度的 12–18%，可设置）。
- `scaleHint` 记录该片段相对基准的缩放系数，渲染时统一应用。

### 7.5 拖拽
- 拖拽时进入"悬空/被抱"状态片段（若有）；缺失则保持当前帧 + 跟随光标。
- 松手：若有"落回"片段则播放，随后回到地面线、恢复 FSM。

---

## 8. 动作连续性：锚定姿态中转机制（核心）

### 8.1 原理
所有非循环片段以**锚定姿态**（端坐）起止。状态切换不直接 A→B，而是：
```
当前片段 → [收尾到锚定] → 锚定(可短暂停留) → [目标片段从锚定起始] → 目标动作
```
因为起止姿态一致，视觉上等价于"宠物回到坐姿再开始下一个动作"，掩盖了片段切换。

### 8.2 循环片段的处理
- 循环片段（理毛/睡眠/趴卧）自身无缝 loop，进入/退出时经锚定中转：`锚定 → 进入过渡(起身/趴下) → 循环 → 退出过渡 → 锚定`。
- 入库时循环片段需配套"进入/退出过渡"片段（见 §4.4 过渡项）。

### 8.3 兜底过渡
- 若锚定中转仍显微硬（快速交互时），在切换点加 60–120ms 极短淡入/位置缓动作为最后兜底（不依赖额外素材）。

---

## 9. 行为引擎（确定性状态机）

### 9.1 状态集合（四大类展开）
- **基础生命**：`idle_sit`(锚定) / `stand` / `walk` / `lie` / `sleep` / `groom` / `turn`
- **互动响应**：`petted`(被抚摸) / `clicked`(点击呼应) / `called`(呼唤) / `dragged`(拖拽)
- **个性招牌**：`signature_*`（用户定义，低频偶发）
- **情绪/需求**：`beg_food` / `drink` / `want_play` / `bored` / `happy`

### 9.2 状态机主干（文字）
```
                  ┌──────────────────────────────┐
                  ▼                              │
[sleep] ⇄ [lie] ⇄ [idle_sit(锚定)] ⇄ [stand] → [walk] → 边缘 → [turn] → [walk]…
                  │    ↑      │
                  │    └──────┘ (经锚定中转进入各类)
                  ▼
            [groom]/[signature_*]/[emotion_*]  (按概率/需求偶发，结束后回锚定)

交互触发（抢占）：
  抚摸→[petted]  点击→[clicked]  呼唤→[called]  拖拽→[dragged]
  （结束后回锚定，恢复 FSM）
```

### 9.3 转移驱动
- **概率转移**：每个状态有一组带权出边（权重由性格 §9.6 调制）。
- **需求驱动**：饥饿/疲劳等数值越高，对应状态（讨食/睡眠）权重越大。
- **节律**：昼夜时段调制（夜间睡眠权重高）。
- **交互抢占**：用户交互立即抢占当前状态。
- **调度微随机化**（§9.5）：在权重采样上叠加随机扰动。

### 9.4 需求模型（提案）
| 需求 | 范围 | 衰减 | 高位触发 |
|---|---|---|---|
| 饥饿 | 0–100 | 按真实时间缓慢上升（有上限，离线不惩罚到极端） | 讨食 |
| 疲劳 | 0–100 | 活动累积 / 夜间上升 | 睡眠 |
| 愉悦 | 0–100 | 交互（抚摸/玩耍）提升，闲置缓慢回落 | 开心/无聊 |
| 注意力 | 0–100 | 长时间无交互下降 | 求玩/呼唤 |

- **离线处理（提案）**：需求按真实时间推进但有上下限保护；重启不出现"宠物濒死"的惩罚性状态，避免情感压力。

### 9.5 调度微随机化（反循环感，四策并用）
1. **多变体**：同状态多片段随机抽取。
2. **稀有动作**：低概率（如 3–8%）插入 signature/哈欠/伸懒腰。
3. **全局呼吸缩放**：§6.3。
4. **微随机**：播放速率 ±5%、静止时长抖动、出现位置 x 抖动、片段顺序打乱。
- **变体耗尽兜底**：长时间闲置变体用尽时，回退最常见变体并拉长切换间隔，避免明显重复。

### 9.6 性格参数化（提案）
引导式问卷采集 **5 维气质**（0–1）：`活泼 / 慵懒 / 粘人 / 胆小 / 好奇`，映射为：
- 转移权重调制：活泼↑→walk/play 权重↑、idle 权重↓；慵懒↑→sleep/lie 权重↑。
- 需求衰减速率：活泼↑→疲劳↑快、愉悦回落快。
- 交互反应：粘人↑→被抚摸愉悦增益↑、注意力下降更快；胆小↑→交互后更易回到隐蔽/静止。
- signature 触发频率：好奇↑→稀有动作概率↑。

---

## 10. 交互层

| 交互 | 触发 | 行为 |
|---|---|---|
| 鼠标抚摸 | 光标进入身体命中盒并移动 | 抢占→`petted` 享受片段 + 愉悦↑ + 偶发呼噜声 |
| 点击/双击 | 点击身体 | 抢占→`clicked` 抬头/看镜头呼应 + 注意力↑ |
| 拖拽 | 按住身体拖动 | 进入 `dragged`（悬空/跟随），松手回地面 |
| 喂食/玩具 | 右键菜单或小面板 | 触发 eat/play 片段 + 饥饿↓/愉悦↑ |

- 右键菜单：喂食 / 给玩具 / 暂时隐藏 / 设置 / 关于。
- **始终置顶（决策）**：默认任何场景都不自动隐藏。
- **安全阀（提案，强烈建议）**：全局快捷键（如 `Ctrl+Shift+H`）临时隐藏宠物 30 分钟或至再按，用于屏幕共享/会议/游戏。不改"默认置顶"决定，仅防社死。

---

## 11. 音频设计（节律随机环境声）

### 11.1 策略
- **环境声**：按昼夜节律随机偶发（白天频率较高、夜间安静），频率/音量可调。模拟"它在过自己的生活，偶尔出声"。
- **动作触发声**：讨食/玩耍/被抚摸等动作叠加对应采样。
- 音频与视频**分离入库**（一段视频可关联一个音频 id，也可静音播放）。

### 11.2 防噪音
- 同一声效**多采样**轮播；每次发声后设**冷却时间**与**单位时间上限**。
- 全局静音开关；默认音量偏低。
- 不干扰系统音频（仅自身播放，不做 ducking）。

---

## 12. 数据与持久化

### 12.1 项目格式（提案）
一个 pet 项目 = 一个目录：
```
<pet_name>/
  persona.json        # 性格5维 + 名字 + 对称性等
  needs-state.json    # 当前需求数值（跨会话）
  behavior-config.json# FSM权重覆盖/节律设置
  clips/              # *.webm 片段
  clips.meta.json     # 片段元数据数组（§5.4）
  audio/              # 音频素材
  audio.meta.json
```

### 12.2 多宠物
- 支持多 profile，托盘切换；同一时刻桌面一只（或设置允许多只共处——后期）。

### 12.3 备份 / 导入导出
- 项目目录可直接复制备份/迁移（情感数据，重要）。UI 提供导出 zip / 导入。

### 12.4 外壳
- 开机自启（默认开，可关）、系统托盘图标、设置面板（显示器/尺度/音量/节律/性格/自启/快捷键）。

---

## 13. 可靠性与边界情况

| 情况 | 处理 |
|---|---|
| 始终置顶遮挡（会议/游戏） | 安全阀快捷键临时隐藏（§10）；未来可加勿扰模式 |
| 缺关键状态片段 | 占位（端坐）+ 清单标红提醒补拍（§5.5） |
| 抠像质量差 | 预览拦截，引导调背景/光/重拍 |
| 长时闲置变体耗尽 | 回退最常见变体 + 拉长间隔（§9.5） |
| 崩溃 | 重启恢复上次 needs-state 与 FSM 状态 |
| 分辨率/显示器热插拔 | 重新计算地面线与尺度，宠物回到可见区 |
| 素材库为空 | 不渲染宠物，弹引导采集，绝不崩溃 |
| 宠物在屏幕外 | 行走片段到边自动转身；异常位置启动时校正回地面线 |

---

## 14. 性能预算（目标）

- **空闲 CPU**：≤ 3%（单段循环解码 + 低频调度）。
- **内存**：< 200MB（Electron 基线 + 单窗口）。
- **解码**：仅解码当前单段；优先硬解；不预解码全部片段。
- **磁盘**：单 pet 项目估算数十～数百 MB（视片段数与码率）；提供码率/分辨率档位。
- **电池**：空闲时调度间隔拉长、呼吸振荡降频。

---

## 15. MVP 分阶段路线图

> 野心完整版按下列阶段渐进交付，每阶段都可独立运行。

- **Phase 0｜渲染打通**：透明置顶窗口 + 单段 WebM-alpha 循环播放 + 全局快捷键隐藏。验证 Electron 透明 alpha 视频可行。
- **Phase 1｜核心闭环**：色键入库管线 + 清单引导导入/打标 + 锚定中转 FSM（仅基础生命状态）+ 底部行走/转身 + 尺度归一化 + 静音。**此时已是一只像样的桌面宠物。**
- **Phase 2｜交互与生命感**：抚摸/点击/拖拽 + 需求模型 + 性格参数化 + 节律 + 调度微随机 + 多变体/稀有动作 + 全局呼吸。
- **Phase 3｜音频与养成**：节律环境声 + 喂食/玩具 UI + 动作触发声。
- **Phase 4｜打磨**：多宠物/备份导出/设置面板/性能调优/抠像质量提升。

---

## 16. 开放问题与待验证假设（风险）

1. **色键鲁棒性**：特定毛色（纯白/纯黑）与背景对比度不足时抠像失败——需 Phase 0 实测，可能需推荐背景色或加 AI 辅助。
2. **WebM-alpha 硬解**：目标 Windows 机器 VP9-alpha 硬解可用性——Phase 0 验证；不可用则降级 VP8-alpha 或软解。
3. **Electron 常驻占用**：用户对 ~150MB 内存的接受度——若敏感，Phase 4 评估迁 Tauri。
4. **锚定中转在快速交互下的观感**：频繁抚摸时反复回锚定是否违和——需实测，可能加交互态直连快路径。
5. **不对称花纹双方向采集可行性**：能否拍到转身/双向行走——若不可，则该宠物限定单向活动范围。
6. **稀有动作/招牌的"偶发不重复感"**：素材量有限时长期运行仍可能被识破循环——靠补拍持续缓解（在世可补拍是最大优势）。

---

## 附录 A：状态→片段需求矩阵（最小可用集）

| 状态 | 必拍片段 | 方向 | 循环 | 变体建议 |
|---|---|---|---|---|
| idle_sit(锚定) | 端坐 | 正面 | 否 | 2–3 |
| stand | 站立 | 侧面 | 否 | 1 |
| walk | 行走 | 左+右 | 否 | 各1–2 |
| lie | 趴卧 | — | 是 | 2 |
| sleep | 睡眠 | — | 是 | 1–2(+翻身) |
| groom | 理毛 | — | 是 | 1 |
| turn | 转身 | 双向 | 否 | 1 |
| petted | 被抚摸 | — | 否 | 1–2 |
| clicked | 抬头呼应 | — | 否 | 1 |

## 附录 B：术语表

- **视频精灵（Video Sprite）**：以真实视频片段为帧源、按需播放合成的 2D 角色。
- **锚定姿态**：所有片段统一的中性起止姿态，用于掩盖切换。
- **色键**：按背景纯色生成透明 alpha 的抠像法。
- **签名/招牌动作**：该宠物独有、低频偶发的个性化片段。
